{
  "name": "workspace",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "astro dev --host --port 5000 --allowed-hosts true",
    "start": "astro dev --host --port 5000 --allowed-hosts true",
    "build": "astro build",
    "preview": "astro preview --host --port 5000 --allowed-hosts true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.13.4",
    "tailwindcss": "^3.4.17"
  }
}
